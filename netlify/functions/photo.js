// photo.js — no longer needed with GitHub approach (images served directly from GitHub raw URLs)
// Kept as stub to avoid 404 on old references

exports.handler = async function () {
  return { statusCode: 404, body: "Use GitHub raw URLs directly" };
};
